{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Iskra Delta Partner Emulator (GDP)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/bin/idp-emu",
      "args": [
        "--model", "gdp",
        "--rom", "${workspaceFolder}/roms/partner_gdp.rom",
        "--hdd", "${workspaceFolder}/disks/TrdiDisk.img"
      ],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": true,
      "MIMode": "gdb",
      "miDebuggerPath": "/usr/bin/gdb",
      "setupCommands": [
        {
          "description": "Enable pretty-printing for gdb",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ],
      "preLaunchTask": "CMake Build"
    },
    {
      "name": "Debug Iskra Delta Partner Emulator (CRT)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/bin/idp-emu",
      "args": [
        "--model", "crt",
        "--rom", "${workspaceFolder}/roms/partner_crt.rom",
        "--disk", "${workspaceFolder}/disks/boot.img"
      ],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": true,
      "MIMode": "gdb",
      "miDebuggerPath": "/usr/bin/gdb",
      "setupCommands": [
        {
          "description": "Enable pretty-printing for gdb",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ],
      "preLaunchTask": "CMake Build"
    }
  ]
}
