This directory contains non-system shared libraries when a platform build
cannot link them statically. Release builds prefer static third-party
libraries, so the directory may otherwise contain only this file.
